# In development: leave VITE_API_BASE_URL unset (or empty).
# The Vite dev server proxies /api/v1/* to http://localhost:8080 automatically.
#
# In production: set this to the API host only — no trailing slash, no /api/v1 path.
# VITE_API_BASE_URL=https://api.example.com
